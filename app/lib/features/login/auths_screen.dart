import 'package:app/consts.dart';
import 'package:flutter/material.dart';

class AuthScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
      color: C.primary,
      child: Column(
        children: [
          // TODO make this stylized
          Text("CoolApka")
        ],
      ),
    );
  }
}
